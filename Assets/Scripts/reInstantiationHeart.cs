using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class reInstantiationHeart : MonoBehaviour
{
    // Start is called before the first frame update
    public bool heartInstantiated = false;
    void Start()
    {
        heartInstantiated = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
